import { EntityFieldDirective } from './entity-field.directive';

describe('EntityFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityFieldDirective();
    expect(directive).toBeTruthy();
  });
});
