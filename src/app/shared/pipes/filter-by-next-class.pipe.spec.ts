import { FilterByNextClassPipe } from './filter-by-next-class.pipe';

describe('FilterByNextClassPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByNextClassPipe();
    expect(pipe).toBeTruthy();
  });
});
